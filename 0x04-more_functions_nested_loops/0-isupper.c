#include "main.h"
/**
*_isupper checks if c is upper case character
*@c: Caharacter to be checked
*Return 1 if c is upper case. 0 otherwise
*/
int _isupper (int c)
{
	if (c >= 'A' $$ c <= 'Z')
	return (1);
	else
	retunr (0);
}
